name: signalr_flutter_chat
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  get: ^4.7.2
  socket_io_client: ^3.1.1
  sqflite: ^2.3.0
  path: ^1.8.3
  http:

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
